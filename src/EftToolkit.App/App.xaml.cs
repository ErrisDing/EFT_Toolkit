namespace EftToolkit.App;

/// <summary>
/// WPF entry point. Both <c>System.Windows</c> and <c>System.Windows.Forms</c> are imported because
/// the tray icon uses WinForms, so the base type is always spelled out here.
/// </summary>
public partial class App : System.Windows.Application
{
}
